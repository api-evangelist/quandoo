naftiko: 1.0.0-alpha2
info:
  label: Quandoo Public Partner API — Reservation Settings
  description: 'Reservation Settings surface of the Quandoo Public Partner API. 1 operations. Lead operation: Quandoo Get Merchant Reservation Settings. Self-contained Naftiko capability covering one Quandoo business surface.'
  tags:
  - Quandoo
  - Reservations
  - Reservation Settings
  created: '2026-06-03'
  modified: '2026-06-03'
binds:
- namespace: env
  keys:
    QUANDOO_AUTH_TOKEN: QUANDOO_AUTH_TOKEN
capability:
  consumes:
  - type: http
    namespace: quandoo-public-partner-api-reservation-settings
    baseUri: https://public-api.prod.quandoo.com
    description: Quandoo Public Partner API — Reservation Settings business capability. Self-contained, no shared references.
    authentication:
      type: apikey
      key: X-Quandoo-AuthToken
      value: '{{env.QUANDOO_AUTH_TOKEN}}'
      placement: header
    resources:
    - name: merchants-merchantid-reservation-settings
      path: /v1/merchants/{merchantId}/reservation-settings
      operations:
      - name: reservationSettings_1
        method: GET
        description: Quandoo Get Merchant Reservation Settings
        inputParameters:
        - name: merchantId
          in: path
          type: integer
          required: true
          description: Id of the merchant
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: quandoo-public-partner-api-reservation-settings-rest
    port: 8080
    description: REST adapter for Quandoo Public Partner API — Reservation Settings. One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/merchants/{merchantid}/reservation-settings
      name: merchants-merchantid-reservation-settings
      description: REST surface for merchants-merchantid-reservation-settings.
      operations:
      - method: GET
        name: reservationSettings_1
        description: Quandoo Get Merchant Reservation Settings
        call: quandoo-public-partner-api-reservation-settings.reservationSettings_1
        with:
          merchantId: rest.merchantId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: quandoo-public-partner-api-reservation-settings-mcp
    port: 9090
    transport: http
    description: MCP adapter for Quandoo Public Partner API — Reservation Settings. One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: get-merchant-reservation-settings
      description: Quandoo Get Merchant Reservation Settings
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: quandoo-public-partner-api-reservation-settings.reservationSettings_1
      with:
        merchantId: tools.merchantId
      outputParameters:
      - type: object
        mapping: $.
