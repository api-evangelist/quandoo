naftiko: 1.0.0-alpha2
info:
  label: Quandoo Public Partner API — Status
  description: 'Status surface of the Quandoo Public Partner API. 1 operations. Lead operation: Quandoo Provides Information on the Status of the Service. Self-contained Naftiko capability covering one Quandoo business surface.'
  tags:
  - Quandoo
  - Reservations
  - Status
  created: '2026-06-03'
  modified: '2026-06-03'
binds:
- namespace: env
  keys:
    QUANDOO_AUTH_TOKEN: QUANDOO_AUTH_TOKEN
capability:
  consumes:
  - type: http
    namespace: quandoo-public-partner-api-status
    baseUri: https://public-api.prod.quandoo.com
    description: Quandoo Public Partner API — Status business capability. Self-contained, no shared references.
    authentication:
      type: apikey
      key: X-Quandoo-AuthToken
      value: '{{env.QUANDOO_AUTH_TOKEN}}'
      placement: header
    resources:
    - name: status
      path: /v1/status
      operations:
      - name: getStatusByGet_1
        method: GET
        description: Quandoo Provides Information on the Status of the Service
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: quandoo-public-partner-api-status-rest
    port: 8080
    description: REST adapter for Quandoo Public Partner API — Status. One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/status
      name: status
      description: REST surface for status.
      operations:
      - method: GET
        name: getStatusByGet_1
        description: Quandoo Provides Information on the Status of the Service
        call: quandoo-public-partner-api-status.getStatusByGet_1
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: quandoo-public-partner-api-status-mcp
    port: 9090
    transport: http
    description: MCP adapter for Quandoo Public Partner API — Status. One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: provides-information-status-service
      description: Quandoo Provides Information on the Status of the Service
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: quandoo-public-partner-api-status.getStatusByGet_1
      outputParameters:
      - type: object
        mapping: $.
