naftiko: 1.0.0-alpha2
info:
  label: Quandoo Public Partner API — Reservations Tags
  description: 'Reservations Tags surface of the Quandoo Public Partner API. 1 operations. Lead operation: Quandoo Get Merchant Reservation Tags. Self-contained Naftiko capability covering one Quandoo business surface.'
  tags:
  - Quandoo
  - Reservations
  - Reservations Tags
  created: '2026-06-03'
  modified: '2026-06-03'
binds:
- namespace: env
  keys:
    QUANDOO_AUTH_TOKEN: QUANDOO_AUTH_TOKEN
capability:
  consumes:
  - type: http
    namespace: quandoo-public-partner-api-reservations-tags
    baseUri: https://public-api.prod.quandoo.com
    description: Quandoo Public Partner API — Reservations Tags business capability. Self-contained, no shared references.
    authentication:
      type: apikey
      key: X-Quandoo-AuthToken
      value: '{{env.QUANDOO_AUTH_TOKEN}}'
      placement: header
    resources:
    - name: merchants-merchantid-reservation-tags
      path: /v1/merchants/{merchantId}/reservation_tags
      operations:
      - name: getReservationTags_1
        method: GET
        description: Quandoo Get Merchant Reservation Tags
        inputParameters:
        - name: merchantId
          in: path
          type: integer
          required: true
          description: Id of the merchant
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: quandoo-public-partner-api-reservations-tags-rest
    port: 8080
    description: REST adapter for Quandoo Public Partner API — Reservations Tags. One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/merchants/{merchantid}/reservation-tags
      name: merchants-merchantid-reservation-tags
      description: REST surface for merchants-merchantid-reservation-tags.
      operations:
      - method: GET
        name: getReservationTags_1
        description: Quandoo Get Merchant Reservation Tags
        call: quandoo-public-partner-api-reservations-tags.getReservationTags_1
        with:
          merchantId: rest.merchantId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: quandoo-public-partner-api-reservations-tags-mcp
    port: 9090
    transport: http
    description: MCP adapter for Quandoo Public Partner API — Reservations Tags. One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: get-merchant-reservation-tags
      description: Quandoo Get Merchant Reservation Tags
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: quandoo-public-partner-api-reservations-tags.getReservationTags_1
      with:
        merchantId: tools.merchantId
      outputParameters:
      - type: object
        mapping: $.
